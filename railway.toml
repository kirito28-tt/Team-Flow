[build]
builder = "nixpacks"

[deploy]
startCommand = "node --experimental-sqlite backend/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
